///TPP_link_get_hover()
/*

Returns: (String) Name / identifier of the link the cursor is hovering over
          or and empty string if the cursor isn't hovering over any links.

*/

return global.TPP_LINK_HOVER;
