///pfd_read(String)
/*

*/
var;

global.pfd_grid=str_to_mp_grid(argument0,GLOBAL.pfd_size,GLOBAL.pfd_size);

/*var g,gg;

g=ds_grid_create(0,0);
gg=global.pfd_grid;

ds_grid_read(g,argument0);

for (t=0; t<ds_grid_height(g); t+=1) {
 for (i=0; i<ds_grid_width(g); i+=1) {
 
 if (g[#i,t]=-1)
  mp_grid_add_cell(gg,i,t);
 
 }
}

ds_grid_destroy(g);*/


