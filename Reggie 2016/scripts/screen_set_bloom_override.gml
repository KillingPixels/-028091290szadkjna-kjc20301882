///screen_set_bloom_override(Intensity, Radius)
/*

Will set new values which will last only for the current step.
(See screen_set_bloom(...))

*/

obj_game_controller.bloomOvrIntensity=argument0;
obj_game_controller.bloomOvrRadius=argument1;
